#include <stdio.h>
#include <studlib.h>

int main() {
    int num1, num2;

    printf("Informe o primeiro número inteiro: ");
    scanf("%d", &num1);

    printf("Informe o segundo número inteiro: ");
    scanf("%d", &num2);

    if (num1 == nume2) {
        printf("Os dois números são iguais: %d\n", numero1);
    } else if (num1 > num2) {
        printf("O primeiro número (%d) é o maior e o segundo número (%d) é o menor.\n", numero1, numero2);
    } else {
        printf("O segundo número (%d) é o maior e o primeiro número (%d) é o menor.\n", numero2, numero1);
    }

    return 0;
}
